const TestPage = () => {
    // load api data
    // load from local storage
    // validate data
    // validate user - does token exists

    return (
        <div>
            <h1>Test Page</h1>
        </div>
    );
};

export default TestPage;